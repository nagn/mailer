<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      A QR code has been scanned for the following label <%= @report.label %>.
    </p>
    <p>
      This QR code is a <%= @report.tag_type %> label.
    </p>
    <p>
    <% if @report.description.length > 0%>
      The user provided the following description of the incident:
      <%= @report.description %> 
    <% else %>
      The user did not leave a description of the incident.
    <% end %>
    </p>
    <p>
      <% if @report.yale_email.length > 0%>
        The user left the following email to be contacted back:
        <%= @report.yale_email %> 
      <% else %>
        The user did not leave their email.
      <% end %>
    </p>
    <p>
      This Email has been automatically generated through mailer.
    </p>
  </body>
</html>