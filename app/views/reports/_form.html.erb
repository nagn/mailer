<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
   
  <% end %>
  
  <div class = "label">
    <%= f.label :label%>
    <%= f.text_field :label, :value => params[:label]%>
  </div>
  <div class = "tag_type">
    <%= f.label :tag_type%>
    <%= f.select :tag_type, options_for_select(Report.tag_types.keys.to_a, params[:tag_type])%>
  </div>
  <div class = "description_label">
    <%= f.label :description%>
    
  </div>
  <div class = "description_textarea">
    <%= f.text_area :description%>
  </div>
  
  <div class="actions">
    <div class = "submit">
      <%= f.submit %>
    </div>
  </div>
<% end %>
